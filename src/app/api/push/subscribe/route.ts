import { createClient } from "@supabase/supabase-js";
import { NextRequest } from "next/server";
export async function POST(request:NextRequest){try{const body=await request.json();const endpoint=String(body?.endpoint||""),p256dh=String(body?.keys?.p256dh||""),auth=String(body?.keys?.auth||"");if(!endpoint.startsWith("https://")||!p256dh||!auth)return Response.json({error:"Assinatura inválida"},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)return Response.json({error:"Serviço indisponível"},{status:503});const db=createClient(url,key,{auth:{persistSession:false}});const {error}=await db.from("push_subscriptions").insert({endpoint,p256dh,auth,user_agent:request.headers.get("user-agent")?.slice(0,500)||""});if(error&&error.code!=="23505")throw error;return Response.json({ok:true});}catch{return Response.json({error:"Não foi possível ativar os alertas"},{status:500})}}
