"use client";
import Image from "next/image";
import {useMemo,useState} from "react";
import {ArrowUpRight,BadgePercent,Search,ShieldCheck,SlidersHorizontal,Store} from "lucide-react";
import type {Deal} from "@/lib/deals";
import {SiteHeader} from "./site-header";
const brl=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"});
export function DealsBrowser({deals}:{deals:Deal[]}){const[query,setQuery]=useState("");const[minimum,setMinimum]=useState(0);const shown=useMemo(()=>deals.filter(deal=>deal.discount>=minimum&&deal.title.toLocaleLowerCase("pt-BR").includes(query.toLocaleLowerCase("pt-BR"))),[deals,minimum,query]);return <><SiteHeader/><main className="deals-page"><section className="deals-hero"><div><p className="eyebrow"><BadgePercent/> Radar brasileiro de preços</p><h1>Promoções que<br/><em>valem o clique.</em></h1><span>Preços nacionais em reais, compra direta na loja e atualização automática. Jogos grátis continuam em destaque na página inicial.</span></div><div className="deal-promise"><ShieldCheck/><strong>Região Brasil</strong><span>Nada de conversão aproximada: exibimos o preço disponibilizado para o mercado brasileiro.</span></div></section><section className="deal-channels" aria-label="Lojas do radar"><strong>Agora no radar</strong><span className="ready"><i/>Steam ao vivo</span><span>Nuuvem em preparação</span><span>Epic em preparação</span><span>GOG em preparação</span><small>Marketplaces de chaves serão identificados separadamente.</small></section><section className="deals-catalog"><div className="section-title"><div><p>Atualizadas automaticamente</p><h2>Melhores ofertas agora</h2></div><span>{shown.length} promoções</span></div><div className="deals-toolbar"><label><Search/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Buscar uma promoção..." aria-label="Buscar promoções"/></label><div><SlidersHorizontal/>{[0,50,70,80].map(value=><button type="button" key={value} className={minimum===value?"selected":""} onClick={()=>setMinimum(value)}>{value?`${value}% ou mais`:"Todas"}</button>)}</div></div>{shown.length?<div className="deals-grid">{shown.map(deal=><article className="deal-card" key={deal.id}><div className="deal-image"><Image src={deal.imageUrl} alt={`Capa de ${deal.title}`} fill sizes="(max-width: 650px) 100vw, (max-width: 1000px) 50vw, 33vw"/><b>-{deal.discount}%</b></div><div className="deal-body"><div className="deal-store"><Store/><span>{deal.store}</span><small>Ativa na {deal.activation}</small></div><h3>{deal.title}</h3><div className="deal-price"><div><s>{brl.format(deal.originalPrice)}</s><strong>{brl.format(deal.salePrice)}</strong></div><span>Preço brasileiro</span></div><a href={deal.url} target="_blank" rel="noreferrer">Ver na loja <ArrowUpRight/></a></div></article>)}</div>:<div className="deals-empty"><BadgePercent/><h3>As ofertas estão sendo atualizadas</h3><p>Tente novamente em alguns instantes ou altere os filtros.</p></div>}</section></main></>}
