import { ExternalLink } from "lucide-react";
import type { Platform } from "@/lib/platforms";
import { SiteHeader } from "./site-header";
export function PlatformCatalog({platforms}:{platforms:Platform[]}){return <><SiteHeader/><main className="platform-page"><div className="page-intro"><p className="eyebrow">Fontes do radar TrioBrabo</p><h1>Onde caçamos<br/><em>os drops.</em></h1><p>Consulte lojas, launchers, assinaturas e comunidades monitoradas. Steam e Epic recebem prioridade, sem ignorar as demais.</p></div><section className="sources"><div className="source-featured">{platforms.filter(p=>p.featured).map(p=><a key={p.slug} href={p.website_url} target="_blank" rel="noreferrer"><div><small>Destaque principal</small><h3>{p.name}</h3><p>{p.note}</p></div><ExternalLink/></a>)}</div><div className="source-grid">{platforms.filter(p=>!p.featured).map(p=><a key={p.slug} href={p.website_url} target="_blank" rel="noreferrer"><div><span className={`source-dot source-${p.kind.toLowerCase().replaceAll("-","")}`}/><strong>{p.name}</strong></div><small>{p.requires_subscription?"Requer assinatura":p.kind}</small><p>{p.note}</p><ExternalLink size={15}/></a>)}</div></section></main></>}
